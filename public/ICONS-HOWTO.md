# 🎨 Как создать иконки для сайта

У вас уже есть **`logo.png`** в корне проекта! Теперь нужно создать из него иконки разных размеров.

## 📋 Необходимые файлы:

1. **`/public/favicon.ico`** (32×32)
2. **`/public/icon-192.png`** (192×192)
3. **`/public/icon-512.png`** (512×512)
4. **`/public/apple-touch-icon.png`** (180×180)
5. **`/public/og-image.png`** (1200×630) — с текстом "КредитПлан"

---

## 🚀 Самый быстрый способ:

### Вариант 1: Онлайн (рекомендуется)
1. Откройте https://realfavicongenerator.net/
2. Загрузите `logo.png`
3. Скачайте готовый пакет
4. Распакуйте файлы в `/public/`

### Вариант 2: Locальная утилита (ImageMagick)

**Windows (PowerShell):**
```powershell
cd public
magick ../logo.png -resize 192x192 icon-192.png
magick ../logo.png -resize 512x512 icon-512.png
magick ../logo.png -resize 180x180 apple-touch-icon.png
magick ../logo.png -resize 32x32 favicon.ico
```

**Mac/Linux (bash):**
```bash
cd public
convert ../logo.png -resize 192x192 icon-192.png
convert ../logo.png -resize 512x512 icon-512.png
convert ../logo.png -resize 180x180 apple-touch-icon.png
convert ../logo.png -resize 32x32 favicon.ico
```

---

## ✅ Проверка

После создания файлов:
1. Перезапустите `npm run dev`
2. Откройте http://localhost:3000
3. Проверьте вкладку браузера — должен быть favicon
4. Откройте http://localhost:3000/favicon.ico — должна быть иконка

---

**Статус:** ⚠️ Иконки нужно создать вручную.  
**Источник:** `logo.png` в корне проекта
